<!-- Comments -->
@model IEnumerable<EntityLayer.Comment>
<div>
    <h2 class="tm-color-primary tm-post-title">Comments</h2>
    @{
        foreach (var item in Model)
        {
            <hr class="tm-hr-primary tm-mb-45">
            <div class="tm-comment tm-mb-45">
                <h3>@item.CommentSubject</h3>
                <figure class="tm-comment-figure">
                    <img src="/img/comment-1.jpg" alt="Image" class="mb-2 rounded-circle img-thumbnail">
                    <figcaption class="tm-color-primary text-center">Mark Sonny</figcaption>
                </figure>
                <div>
                    <p>@item.CommentContent</p>
                    <div class="d-flex justify-content-between">
                        <span class="tm-color-primary">@(((DateTime)item.CommentDate).ToString("dd-MMM-yyyy"))</span>
                    </div>
                </div>
            </div>

        }
    }
</div>
